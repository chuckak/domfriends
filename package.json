{
  "name": "domfriends",
  "version": "0.0.1",
  "description": "DOM friends and friends",
  "main": "index.js",
  "scripts": {
    "start": "rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chuck Aksamit",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.57.1"
  }
}
